let actions = require('./Tasks'); //needs to be called to update prototypes. otherwise unused
let archetects = require('./archetechts');
let managers = require('./Managers');
let config = require('../.screeps.json');


module.exports.loop = function () {
    let debug_status = config.debug


}